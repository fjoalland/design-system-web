version: '3'
services:
  site:
    command: gulp serve
    # Use on etime "npm rebuild node-sass --force" if you have an error 
    # or npm install about linux version of node-sass
    build: .
    volumes:
      - ./..:/app
      - ./cache/bundle:/usr/local/bundle
    ports:
      - ${PORT}:4000